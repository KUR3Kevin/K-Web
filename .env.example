# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
# For local MongoDB: mongodb://localhost:27017/tech-news
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/tech-news
# IMPORTANT: Use strong, unique credentials for production
MONGODB_URI=mongodb://localhost:27017/tech-news

# Admin Credentials
# SECURITY: Change these before deploying to production
ADMIN_USERNAME=admin

# Generate secure password hash using bcrypt:
# node -e "console.log(require('bcryptjs').hashSync('YOUR-SECURE-PASSWORD-HERE', 10))"
# IMPORTANT: Never commit the actual password, only the hash
ADMIN_PASSWORD_HASH=$2a$10$example.hash.replace.with.your.own

# Session Secret
# CRITICAL SECURITY: Generate a strong random secret (32+ characters)
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# NEVER use the default value in production
# NEVER commit this value to version control
SESSION_SECRET=change-this-to-a-random-secret-key-for-production

# Client URL (for CORS)
# IMPORTANT: Set to your actual domain in production
# For multiple domains, use comma-separated list: https://example.com,https://www.example.com
# Never use 'true' or '*' in production
CLIENT_URL=http://localhost:5000

# PayPal Configuration (optional)
# SECURITY NOTE: Do not use email in frontend forms
# Instead, create a PayPal hosted button and use the button ID
# Get button ID from: https://www.paypal.com/buttons/
PAYPAL_BUTTON_ID=your-paypal-hosted-button-id

# Security Notes:
# 1. Never commit .env file to version control (already in .gitignore)
# 2. Use different values for development and production
# 3. Rotate secrets regularly (every 90 days recommended)
# 4. Use strong, randomly generated values for SESSION_SECRET
# 5. Keep backup of production .env in secure password manager
# 6. Never share .env file via email or messaging apps
