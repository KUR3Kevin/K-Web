{
  "name": "tech-insights",
  "version": "2.0.0",
  "description": "Secure full-stack tech news aggregation platform with React frontend and Express backend",
  "scripts": {
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build": "npm install && npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "cd backend && npm start",
    "postbuild": "echo 'Build complete! Frontend built to frontend/dist/'",
    "deploy": "npm run build && npm start"
  },
  "keywords": [
    "tech-news",
    "react",
    "express",
    "mongodb",
    "security",
    "full-stack"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
